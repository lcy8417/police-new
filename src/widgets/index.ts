// widgets layer — layout composition: app-shell (fixed TopNav + Sidebar +
// Outlet), crime-register-board, pattern-panel, search-results-grid,
// history-view, shoe-detail-view.
// FSD import rule: may import `features`, `entities`, `shared`.
export {};
